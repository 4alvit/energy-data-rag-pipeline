apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: k3s-wildcard-tls
  namespace: energy-rag
spec:
  secretName: energy-rag-k3s-tls
  issuerRef:
    name: letsencrypt-cloudflare
    kind: ClusterIssuer
  dnsNames:
    - "*.k3s.example.com"
